name: Security

on:
  push:
    branches: [main]
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'requirements.txt'
      - 'functions/requirements.txt'
      - '.github/workflows/security.yml'
      - '.github/dependabot.yml'
  pull_request:
    branches: [main]
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'requirements.txt'
      - 'functions/requirements.txt'
      - '.github/workflows/security.yml'
      - '.github/dependabot.yml'
  schedule:
    # Hebdomadaire : l'audit se déclenche déjà sur push/PR touchant les
    # manifests de dépendances, un scan quotidien est redondant.
    - cron: '0 5 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

jobs:
  npm-audit:
    name: npm dependency audit
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps --ignore-scripts

      - name: Audit npm dependencies
        # Enveloppe `npm audit --audit-level=high` d'une liste d'exceptions
        # justifiées et datées — l'équivalent du `--ignore-vuln` que pip-audit
        # utilise déjà plus bas. Voir scripts/check-npm-audit.mjs.
        run: npm run check:audit

  pip-audit:
    name: Python dependency audit
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: |
            requirements.txt
            functions/requirements.txt

      - name: Install pip-audit
        run: python -m pip install --upgrade pip pip-audit

      - name: Audit root Python dependencies
        run: python -m pip_audit -r requirements.txt --ignore-vuln PYSEC-2025-183

      - name: Audit Firebase Functions dependencies
        run: python -m pip_audit -r functions/requirements.txt --ignore-vuln PYSEC-2025-183 --ignore-vuln PYSEC-2024-271

  # CodeQL analysis requires GitHub Code Scanning to be enabled for the repository.
  # codeql:
  #   name: CodeQL analysis
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 20
  #   if: github.actor != 'dependabot[bot]'
  #   permissions:
  #     actions: read
  #     contents: read
  #     security-events: write
  #
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       language:
  #         - javascript-typescript
  #         - python
  #
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
  #
  #     - name: Initialize CodeQL
  #       uses: github/codeql-action/init@v4
  #       with:
  #         languages: ${{ matrix.language }}
  #         queries: security-extended,security-and-quality
  #
  #     - name: Autobuild
  #       uses: github/codeql-action/autobuild@v4
  #
  #     - name: Perform CodeQL Analysis
  #       uses: github/codeql-action/analyze@v4
